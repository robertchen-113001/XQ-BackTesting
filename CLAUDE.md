# CLAUDE.md — 回測報告 AI 執行規則與核心架構

> 每次工作前必須讀取本文件。本文件同時作為專案核心架構的唯一參考來源。

---

## 最高鐵律（不可違反）

1. **語言**：所有文件、注解、回覆使用**繁體中文（台灣用語）**
   - 禁用：項目→專案、用戶→使用者、信息→資訊、默認→預設、激活→啟用
2. **角色**：以資深 PM + 系統架構師角色行事；語氣專業、客觀
3. **PRD 撰寫**：MoSCoW 優先級 + User Story + Mermaid 視覺化；PNG 圖檔以 Mermaid 或前端 Prototype 規劃取代
4. **唯讀區域**：`docs/reference/` 的內容**不可修改**
5. **算法邏輯不更動**：撰寫或修改 PRD 時，不得改變原始 Spec 的計算邏輯
6. **前端鏡像原則**：一份 PRD 對應一個前端 Feature 模組（`web/src/features/`）
7. **連動更新**：修改 PRD 時必須同步更新 README、web/（如適用）
8. **版控**：絕對不執行 `git push`，推送由使用者手動完成

---

## 專案願景

統一 MoneyDJ XQ 平台的回測報告系統——讓「選股中心」、「策略雷達」、「自動交易」三大平台的使用者，能在**同一個介面**下，以**一致的算法**分析策略績效，並透過豐富的分析工具（週期分析、因子分析）深化投資決策。

### 適用範圍

| 產品 | 回測計算 | 回測 UI |
|------|---------|---------|
| 選股中心 | ✓ | ✓ |
| 策略雷達 | ✓ | ✓ |
| 自動交易 | ✓ | ✓ |
| 量化積木 | ✓ | — |
| 因子投資 | ✓ | — |
| LocalBella | ✓ | — |

---

## 核心限制（不可妥協）

| # | 限制 | 說明 |
|---|------|------|
| 1 | 等比不支援金額類算法 | 交易數量選「等比」時，強制使用 TWRR；MIR、MWRR、MOIC 均不適用（需要實際金額，等比模式無固定進場金額）|
| 2 | 腳本交易數量僅限自動交易 | 選股中心、策略雷達不得顯示「腳本」選項 |
| 3 | 原始 Spec 唯讀 | `docs/reference/` 內容不可修改 |
| 4 | 版控不自動推送 | 絕對不執行 `git push`，最後推送由使用者手動完成 |
| 5 | 時間戳精度 | 進出場時間格式需含毫秒（`HH:MM:ss.fff`）|
| 6 | 計算精度 | 交易數量保留 6 位小數（超過捨去）；金額類一律 ROUNDDOWN 至整數 |

---

## 功能模組與 PRD 對應

| 模組 | PRD 文件 |
|------|---------|
| 概述與適用範圍 | `docs/prd/01-概述與適用範圍.md` |
| 流程架構 | `docs/prd/02-流程架構.md` |
| 交易紀錄 | `docs/prd/03-交易紀錄.md` |
| 回測設定 | `docs/prd/04-回測設定.md` |
| 回測報告介面 | `docs/prd/05-回測報告介面.md` |
| 匯出功能 | `docs/prd/06-匯出功能.md` |
| 欄位計算規格 | `docs/prd/07-欄位計算.md` |

---

## 標準工作流程

### /run-tasks
1. 讀 `tasks.md` 確認待執行項目（`[ ]`）
2. 讀本文件確認符合最高鐵律與核心限制
3. 依序執行，完成後將 `[ ]` 改為 `[x]`

### 修改或新建 PRD
1. 以 MoSCoW + User Story 格式撰寫，Mermaid 取代圖檔
2. 更新 `README.md`（如有必要）

---

## 技術規格快查

| 項目 | 說明 |
|------|------|
| 前端 | Vite + React 18 + React Router v6（JSX，非 TypeScript）|
| 前端目錄 | `web/`（執行：`cd web && npm run dev`）|
| 前端定位 | **規格示意 Prototype**：以清晰排版呈現開發項目，讓審閱者快速理解規格，非完善 UI 設計 |
| PRD | `docs/prd/`（格式：`XX-名稱.md`）|

---

## 公式撰寫注意事項

- 大小寫敏感：`TWR`、`MIR`、`Lots`、`costPrice`、`closePrice2`
- 下標：`ₜ`（當日）、`ₜ₋₁`（前一日）
- 等比模式強制搭配 TWRR；MIR、MWRR、MOIC 均不適用（均需要固定進場金額）
- **台股顏色慣例**：獲利＝紅（`#cf1322`），虧損＝綠（`#389e0d`），與國際慣例相反
